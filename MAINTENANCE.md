# Experiment Journal 유지보수 메모

## 역할과 주소

- 편집용: `https://continuingrace.github.io/experiment-journal/`
- 공개용: `https://continuingrace.github.io/experiment-journal-public/`
- 편집용은 작성·저장·백업·복원·발행을 담당한다.
- 공개용은 발행된 완성본을 읽기 전용으로 보여 준다.

## 데이터와 미디어의 기준

- 편집 내용은 기기별 `localStorage`에 저장된다.
- 새로 업로드한 원본 이미지·영상은 해당 기기의 IndexedDB에 저장된다.
- 따라서 데스크톱과 iPhone 홈 화면 편집용은 처음에는 같은 데이터를 자동 공유하지 않는다.
- 공개용의 `release.json`과 `media/`는 마지막으로 발행한 완성본의 기준이다.
- 모바일에서 `Tools → 공개본 동기화`를 실행하면 공개본 데이터로 편집용 데이터를 교체한다.
- v0.3.43부터 동기화한 공개 미디어는 모바일에 다시 복사하지 않고 공개용의 절대 URL을 직접 사용한다. 이 방식은 iPhone 홈 화면의 별도 저장공간 문제를 피한다.

## 안전한 작업 순서

1. 데스크톱 편집용에서 수정한다.
2. `저장` 후 `완성본 저장`을 실행한다.
3. `웹페이지로 발행`으로 공개용을 갱신한다.
4. 모바일 편집용에서는 `Tools → 공개본 동기화`를 실행한다.

공개본 동기화는 모바일의 현재 편집 데이터를 마지막 공개본으로 교체한다. 모바일에만 있는 수정이 있다면 먼저 JSON 백업을 만든다.

## 수정할 때 지킬 원칙

- `experiment-journal-v3` localStorage 키와 `experimentJournalMediaV3` IndexedDB 구조를 임의로 바꾸지 않는다.
- 실험 데이터, 백업/복원, 미디어 업로드, 대표 이미지, 공개 발행 기능을 함께 회귀 점검한다.
- 편집용과 공개용의 역할을 섞지 않는다. 공개용은 공개 데이터만 읽고, 편집용은 사용자의 기기 데이터가 우선이다.
- 모바일 문제를 단순 `overflow: hidden` 또는 글자 축소로 숨기지 않는다.
- 변경 후에는 헤더 버전, `index.html`의 스크립트 캐시 버전, `v014.js`의 버전 값을 함께 맞춘다.
- GitHub Pages 실제 주소에서 새 버전이 내려오는지 확인한 뒤 완료로 처리한다.

## 미디어가 보이지 않을 때 확인 순서

1. 편집용과 공개용의 버전·주소를 분리해 확인한다.
2. 공개용에서 이미지가 보이는지 확인한다.
3. 공개용의 `release.json`에 실험별 `media` 항목이 있는지 확인한다.
4. 모바일 편집용에서 `Tools → 공개본 동기화`를 실행한다.
5. 동기화 뒤에도 보이지 않으면, 동기화 직후의 안내 또는 오류 화면과 헤더 버전이 함께 보이게 캡처한다.

## 이번 이슈에서 확인된 사실

- 공개용 이미지가 정상이라면 발행된 `media/` 파일과 공개 데이터는 정상일 가능성이 높다.
- iPhone 홈 화면의 편집용은 Safari/데스크톱과 별도의 로컬 저장소를 사용할 수 있다.
- 여러 이미지·영상을 모바일 IndexedDB로 일괄 복사하는 방식보다, 이미 공개된 미디어 URL을 직접 표시하는 방식이 이 프로젝트의 공개본 동기화 목적에 더 적합하다.
